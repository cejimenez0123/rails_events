<h1>Evento</h1>
<h3>Your RSVPs:</h3>
<% if !@rsvps.empty?%>
<%= @rsvps.each do |rsvp|%>
    <%= link_to "#{rsvp.event.name}", event_path(rsvp.event)%>
<%end%>
<%else%>
<p>RSVP to some events in the events page<p>
<%end%>
<%= link_to "Events", events_path%>
<%= link_to "Home", user_path(current_user)%>
